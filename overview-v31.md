# DebateSphere v31 — 四层人格引擎

> 版本：v31 · 2026-08-13（v31.1 2026-08-17：**人格连接 AI LLM**）
> 主题：从「提示词工程」升级为「底层人格架构」——让 AI 不再是换皮提示词，而是拥有**内核、状态、记忆**的持续人格。

## 一、为什么做 v31

v30 之前的所有努力都集中在**提示词层**（审题、输入识别 A-E、CoT 格式）。但提示词是「一次性指令」：每一轮对话 AI 都会忘记自己是谁、忘了你们聊过什么、情绪永远从零开始。用户反馈「辩论还是没有按照我的要求来」——根因不是提示词不够多，而是**人格没有可计算的内在状态**。

v31 借鉴 Psyche「内在自我」、OpenHer「情绪热力学」、Soul OS「身份与记忆伴生」的架构思路，把人格从提示词中外化为四层可计算的引擎：

```
┌─────────────────────────────────────────────┐
│ 第1层 人格内核 PersonaDrives（静态·灵魂底色） │  ← 16人格 × 5维驱力 + 渴望/恐惧
├─────────────────────────────────────────────┤
│ 第2层 状态引擎 PersonaEngine（动态·此刻感受） │  ← valence/arousal/energy/intimacy/novelty
├─────────────────────────────────────────────┤
│ 第3层 持久记忆 PersonaMemory（跨会话·记得你） │  ← 偏好/经历/关系 启发式提取 + 摘要沉淀
├─────────────────────────────────────────────┤
│ 第4层 Few-shot 强化（对话模式·稳定输出）      │  ← 16人格对话风格表 + 5组 few-shot
└─────────────────────────────────────────────┘
              ↓ 每轮注入
      系统提示词：人格设定 + 驱力段 + 状态段 + 记忆段
```

## 二、四个新模块

### 1. `data/personaDrives.ts` — 人格内核（静态）
- 16 人格 × 5 维驱力：**连接欲 / 好奇欲 / 求新欲 / 安全欲 / 表达欲**（各 0-1）
- 每人格一句话**核心渴望** + **最深恐惧**
- `getPersonaDrives` 未知类型安全默认；`topDrives` 取最强 2 维供 UI
- `buildDriveSection` 生成「## 你的内在驱力」注入段——不是规则，是灵魂的底层设定

### 2. `utils/personaEngine.ts` — 状态引擎（动态）
- 状态：`valence[-1,1] / arousal / energy / intimacy / novelty`（均 0-1）+ turnCount + lastIntent + lastTopic
- **差异化初始化**：E 型精力高、F 型亲密度高、P 型新鲜度高
- **确定性演化** `updateState(prev, signal)`：意图 A-E 驱动状态变化（A 新话题→novelty↑；B 回应→intimacy↑；C 转移→novelty 重置；D 倾诉→intimacy↑+energy↓+arousal↑；E 求建议→intimacy↑）
- **情绪词感染**：正向/负向词典 + 强度词 → valence 跟随用户情绪微移
- **自我披露检测**：10+ 开头模式 → 关系升温
- 自然回归：每轮 energy -0.015、arousal 向 0.45 回归；clamp + 千分位保证可控可测
- `buildStatePrompt` 生成「## 你此刻的内在状态」——让 AI 感受自己的情绪再说话
- localStorage 持久化（`ds_persona_state_*`），node 测试自动兜底

### 3. `utils/personaMemory.ts` — 持久记忆（跨会话）
- 每人格独立记忆库（`ds_persona_memory_*`），**下次见面 TA 还记得你**
- 四类记忆：事实 / 偏好 / 经历 / 关系
- **启发式提取** `extractMemoryCandidates`：我喜欢/我最近/只有你 等模式 → 不消耗 LLM
- 超限（8 条）自动**摘要沉淀**：按类型各取最新一条，保留最近 3 条细节
- `buildMemorySection` 生成「## 你对 TA 的记忆」——自然流露，不背诵履历

### 4. 提示词层接入（debatePrompts.ts / dialogueMode.ts）
- `buildPersonaSystemPrompt(typeId, topic, side, { state?, memory? })` → 人格 + 驱力 + 状态 + 记忆
- `buildDialogueSystemPrompt(typeId, { state?, memory? })` → 同上 + 对话风格 few-shot
- **向后兼容**：不传 state/memory 时输出与 v30 完全一致

## 三、UI 接入

| 位置 | 内容 |
|------|------|
| **DebateRoom 状态卡** | 每位参与者：情绪 emoji + 描述、精力条、亲密度、话题新鲜度、🧠记忆条数、重置按钮（清空状态+记忆） |
| **DebateRoom 对话模式** | LLM 意图(A-E)驱动状态演化 + 记忆沉淀；arena/模板模式每轮发言后自动演化 |
| **DebateRoom arena** | `runNextSpeech(state, { state, memory })` 透传——辩手带着此刻感受上场 |
| **PersonaChat 头部** | 情绪/精力/亲密度/新鲜度实时行 + 记忆条数 + 重置 |
| **PersonaChat 设置页** | 跨会话记忆展示（类型标签 + 内容）+「忘记我」按钮 |
| **HallPage 人格卡** | 16 人格卡显示各自情绪/精力/亲密度/记忆条数（只读） |

## v31.1 — 人格连接 AI LLM（全模式打通）

> 用户反馈：「你真的都安装到软件里了吗，没有任何改变，还有，人格要连接 aillm」
> 根因：v31 的驱动/状态/记忆**只注入提示词层**，但 1v1（PersonaChat）与 duel/auto 辩论模式**从未调用 LLM**——用户配置了大模型也不生效，看到的永远只是本地模板。

v31.1 把 LLM 接入补全为**全模式**：

| 模式 | v31 之前 | v31.1 之后 |
|------|---------|-----------|
| 💬 对话模式（DebateRoom） | ✅ LLM | ✅ LLM（不变） |
| ⚔️ arena 顶尖辩手 | ✅ LLM | ✅ LLM（不变） |
| 🥊 1v1 往来辩论（PersonaChat） | ❌ 纯模板 | ✅ **LLM 优先**：buildDialogueMessages 注入状态+记忆 → chatCompletion → 解析【理解】+【回应】→ 意图驱动状态演化 + 记忆沉淀 → 流式打字机 + 🧠思考链折叠 |
| ⚡ duel/auto 辩论 | ❌ 纯模板 | ✅ **LLM 优先**：buildSpeechMessages（含审题/资料包/对方发言/状态/记忆）→ parseCoT 解析【思考】+【发言】→ 展示思考链 |

同时新增：
- **AI 连接状态提示**：PersonaChat 对话区顶部显示「⚡ 已连接 AI 大模型」/「🔌 未连接 AI（当前为本地模板人格）· 请到设置页配置 LLM」——用户一眼知道当前是谁在说话
- **LLM 失败自动兜底**：API 出错时回退本地模板，不打断对话
- 未配置 LLM 时所有模式行为与 v30 一致（本地模板），配置后自动升级为真人格

设置页 LLM 配置入口一直在（服务商选择 / API Key / 模型 / 测试连接），v31.1 让它真正生效到每个对话。

## 四、测试与验证

`npm run test:arena:v31` — **69/69 全绿**：

- 🎭 人格内核：16 人格 × 5 维覆盖、数值合法、渴望/恐惧完整、topDrives、默认兜底
- ⚙️ 状态引擎：差异化初始化、意图 A/D/E 演化方向、情绪感染、连续 20 轮不越界、千分位、确定性
- 🧠 持久记忆：三类提取、每轮 ≤3 条、超限摘要沉淀、持久化回读
- 📝 提示词层：辩论/对话双路注入驱力+状态+记忆、不传兼容
- ⚔️ arena 透传：runNextSpeech 携带 persona 上下文正常、模板兜底不破坏
- 🔁 v30/v28 回归：意图识别解析、输入识别规则、CoT、对话模式全部保持

`tsc --noEmit` ✅ 0 错误；`vite build` ✅ 22.4s。

## 五、预览

- Dev server：http://localhost:5175 （建议走一遍：**对话模式** 聊几句看状态卡情绪变化 → 刷新页面看记忆还在 → 换人格看状态独立）

## 六、交付记录（v31.1 已打包）

- ✅ 2026-08-17 v31.1 桌面 exe 已打包交付（LLM 全模式连接）
- ✅ `tsc --noEmit` 0 错误；`vite build` 22.3s；v31 测试 69/69 全绿（v31.1 回归无破坏）
- ⏳ APK 待用户确认桌面版后打包

## 七、技术要点备忘

- **状态写入三同步**：ref（异步回调读取）+ React state（UI 渲染）+ localStorage（持久化）
- **记忆只在对话/1v1 沉淀**：辩论模式只演化状态不沉淀记忆（防止把辩题内容当私人记忆）
- **信号门控**：只有用户最近 2 条消息内有发言时，才把用户文本作为情绪信号注入（避免自动辩论中反复情绪感染）
- **arena 透传零破坏**：persona 参数可选，测试/无 UI 场景不受影响
