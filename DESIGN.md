# DebateSphere 思辩星球 · 设计系统（DESIGN.md v1）

> 本文档是生成与评审 DebateSphere 所有 UI 代码的**唯一权威设计依据**。
> 任何页面、组件、交互不得违反以下规则。开发前先读这里，改 UI 先改这里。
>
> 来源：基于 2026 国际顶级设计基准（Linear / Vercel / Awwwards 获奖站点）实测提炼，
> 融合 DebateSphere 自有品牌（深空星系隐喻）。已针对本项目（Vite + React 18 + Tailwind + framer-motion + Electron/Capacitor）校准，**不照搬任何第三方模板**。

---

## 0. 设计哲学（不可妥协）

1. **星系隐喻即品牌** — 16 种人格 = 16 颗星球。深空是基底，光晕是人格身份，轨道是交互路径。浏览产品 = 探索星系，不是翻阅目录。
2. **系统克制** — 全平台**只有一个交互强调色**（霓虹青 `#22d3ee`）。16 个人格色只出现在人格相关内容（卡片/光晕/图表/对话气泡），**禁止**用作按钮背景、全局装饰、或与强调色争抢视觉。
3. **深色为默认** — OLED 友好、沉浸优先。浅色仅作辅助（设置可切），色彩在任何主题下不得失真。
4. **克制玻璃，拒绝模板** — 毛玻璃只用于导航栏、浮层、悬浮面板（低透明度 + 强模糊）；正文卡片用实色表面。**禁止**：三栏等分网格、卡片左侧彩色竖条、图标堆砌、每卡一个彩虹渐变。

---

## 1. 色彩系统

### 1.1 深空基底 + 玻璃表面体系（国外顶级站点实测配方）

**核心原则：不用实色卡片，用「半透明玻璃叠加」。** 这是国外玻璃质感（Linear / Vercel / Raycast / Stripe）与「AI 感高饱和霓虹」的本质区别——颜色不是发光的，是**蒙在磨砂玻璃后面**的。

| Token | 色值 | 用途 |
|---|---|---|
| `--canvas` | `#05060F` | 页面主背景（带蓝调的深空黑，**禁止纯黑 #000**） |
| `--canvas-glow` | `rgba(99,102,241,0.08)` | 画布左上蓝紫氛围光（全站仅此一层） |
| `--glass-bg` | `rgba(255,255,255,0.045)` | 卡片玻璃基底（**半透明白，不是实色**） |
| `--glass-bg-strong` | `rgba(255,255,255,0.07)` | 悬浮层、模态、导航 |
| `--glass-bg-hover` | `rgba(255,255,255,0.09)` | 悬停/选中 |
| `--glass-border` | `rgba(255,255,255,0.10)` | 玻璃描边（细、亮、统一 1px） |
| `--glass-border-strong` | `rgba(255,255,255,0.16)` | 强调玻璃、激活态 |
| `--glass-highlight` | `inset 0 1px 0 rgba(255,255,255,0.08)` | 玻璃顶边内高光（质感关键） |

**玻璃配方（所有卡片/面板强制）：**
```css
.glass {
  background: var(--glass-bg);
  border: 1px solid var(--glass-border);
  border-radius: 16px;
  backdrop-filter: blur(24px) saturate(140%);
  box-shadow: var(--glass-highlight), 0 8px 32px rgba(0,0,0,.25);
}
```

- 层级靠「玻璃白度递增 + 边框变亮」表达，**大投影仅用于浮层**。
- 星云氛围背景（`deepspace-bg`）保留但**饱和度收敛 50%**、光斑 alpha ≤ 0.25，让内容浮在星空之上。
- **删除**：任何 `#22d3ee` 霓虹青、`#f472b6` 品红的发光按钮——霓虹光效是"AI 模板感"第一来源。

### 1.2 16 色人格光谱（柔和玻璃光谱，唯一权威）

**原则：色相可区分，但整体「低饱和 + 提亮」，像透过磨砂玻璃看彩光。** 所有颜色为 HSL 统一校准：饱和度 55–65%、亮度 68–75%（对比旧版 600 级高饱和色全面降饱和提亮）。**禁止任何纯色/高饱和发光的"霓虹人格"**。

**NT 分析家 · 理性冷色（柔）**

| 人格 | 色值 | 人格 | 色值 |
|---|---|---|---|
| INTJ | `#8F7FF5` 柔紫 | INTP | `#6FA3F5` 柔蓝 |
| ENTJ | `#E87E7E` 柔红 | ENTP | `#E3AC6C` 柔琥珀 |

**NF 外交家 · 温暖柔色（柔）**

| 人格 | 色值 | 人格 | 色值 |
|---|---|---|---|
| INFJ | `#A79BF0` 薰衣草 | INFP | `#E58FB5` 柔粉 |
| ENFJ | `#66C4D4` 柔青 | ENFP | `#E8976F` 柔珊瑚 |

**SJ 守护者 · 大地稳色（柔）**

| 人格 | 色值 | 人格 | 色值 |
|---|---|---|---|
| ISTJ | `#7C8AA3` 钢蓝 | ISFJ | `#69C4B6` 柔绿松石 |
| ESTJ | `#55637F` 海军 | ESFJ | `#E87E93` 柔玫瑰 |

**SP 探险家 · 明亮动色（柔）**

| 人格 | 色值 | 人格 | 色值 |
|---|---|---|---|
| ISTP | `#8C97AA` 灰蓝 | ISFP | `#AD8FE8` 紫丁香 |
| ESTP | `#D9B871` 香槟金 | ESFP | `#6FC29B` 柔薄荷 |

**使用规则：**
- 人格色用于：头像光晕、卡片边框/角标、雷达图/图表、对话气泡身份、技能条。
- 人格色**作为文字**用 200 级更浅变体（如 `#A79BF0`→`#C9C0FA`），保证对比；图形才用表中色值。
- 光晕一律 **alpha ≤ 0.30 的大范围径向渐变**（`{color}4D → transparent`），禁止实色发光。
- 冲突规避：ENTJ（柔红）与 error 语境不同——error 必带 icon 且仅用于操作反馈；ESFP（柔薄荷）与 success 同理。

### 1.3 文本亮度阶梯（Linear 式四阶）

| Token | 色值 | 用途 |
|---|---|---|
| `--ink` | `#F2F4FF` | 标题、主文本 |
| `--ink-2` | `#A6ADCF` | 描述、次级信息 |
| `--ink-3` | `#5F6796` | 时间戳、标注、占位 |
| `--ink-4` | `#3D4363` | 禁用态、弱化 |

### 1.4 功能色（玻璃化：降饱和 + 提亮）

`success #2FC9A3` / `warning #E3B25C` / `error #E57E7E` / `info #6FA3F5`

- 功能色仅用于**图标 + 文字 + 状态点**，禁止大面积背景（那是 AI 模板做法）。
- 与人格色共享"柔和玻璃"观感，全站无一处高饱和刺眼色。

### 1.5 渐变规则（"渐变式简约"核心，玻璃质感版）

| 场景 | 规则 | 示例 |
|---|---|---|
| 背景氛围 | **低饱和低 alpha**，光斑 ≤ 0.25，多层径向叠加，亮度差小 | `radial-gradient(52% 40% at 12% 4%, rgba(99,102,241,.14), transparent 70%)` |
| 人格光晕 | 单色系径向，**alpha ≤ 0.30**，范围大、边缘羽化（磨砂感） | `radial-gradient(circle, {personaColor}40, transparent 70%)` |
| 文字渐变 | 仅 Hero 大标题可用，白→极浅冷色，禁止三色以上 | 白 → `#C7D2FE`（柔蓝紫，非霓虹） |
| 玻璃高光 | 卡片顶边 `inset 0 1px 0 rgba(255,255,255,.08)`，模拟光从上方打在玻璃上 | 见 1.1 `.glass` 配方 |
| **禁止** | 多色渐变按钮、彩虹渐变卡片、发光霓虹描边、每卡一渐变的模板感 | 按钮 = 玻璃底 + 单色柔光晕 |

---

## 2. 排版系统

### 2.1 字体（离线安全，Electron/APK 可打包）

| 用途 | 字体 | 说明 |
|---|---|---|
| 显示/标题 | **Space Grotesk**（本地 woff2） | 替代 Fraunces（拉丁衬线不渲染中文 + 需联网加载），个性与工程兼顾 |
| 正文 | **Inter** + PingFang SC / Microsoft YaHei | 中文兜底 |
| 数据/标签 | **JetBrains Mono** | 数字一律 `font-variant-numeric: tabular-nums` |

### 2.2 字号层级

| 层级 | 字号 | 字重 | 行高 | 字距 |
|---|---|---|---|---|
| Display | 48–64px | 700–800 | 1.05 | `-0.03em`（Linear 式负字距） |
| H2 | 28–36px | 600 | 1.2 | `-0.02em` |
| H3 | 20–24px | 600 | 1.3 | `-0.01em` |
| Body | 15–16px | 400 | 1.6 | 0 |
| Small | 12–13px | 400 | 1.5 | 0 |

### 2.3 文本规则

- 正文左对齐，**禁止居中对齐正文**（标题除外）。
- 段落间距 12–16px；标题优先白或人格浅色。
- 关键数字（得分/百分比）用 Mono + 人格色，可做 CountUp 动效。

---

## 3. 布局系统

### 3.1 网格

| 设备 | 列数 | 间距 | 边距 |
|---|---|---|---|
| 桌面 | 12 | 20–24px | 80–120px |
| 平板 | 8 | 16px | 40–60px |
| 手机 | 4 | 12px | 16–20px |

### 3.2 Bento Grid（人格大厅）

- 大卡跨 6 列（高 320–400px）：星系图 / 热门人格主视觉
- 中卡跨 4 列（高 240–320px）：最新辩论 / 快速匹配
- 小卡跨 3 列（高 160–240px）：人格条目
- 圆角统一 16–20px；内边距 20–24px；卡片间 20–24px
- **不对称但有序**：允许重叠/跨列，但每个气质分组内节奏一致

### 3.3 间距（8px 基准）

`4 / 8 / 12 / 16 / 20 / 24 / 32 / 48 / 64 / 96`

---

## 4. 组件规范

### 4.1 PersonalityCard（人格卡片）— 核心

```
┌──────────────────────────────┐
│  [人格色径向光晕]             │
│  ┌──────┐  INTJ 建筑师        │
│  │ 头像  │  战略家 · 理性      │
│  │ (svg) │  分析家组           │
│  └──────┘                     │
│  ── 核心特质 ──                │
│  [独立] [远见] [果断]          │
│  ▼ 进入星球                   │
└──────────────────────────────┘
```

- 光晕 = 人格色 `radial-gradient`；hover：`translateY(-4px)` + 光晕扩散 + 边框提亮
- 点击涟漪；**禁止**卡片左侧彩色竖条（AI 模板特征）

### 4.2 ChatBubble（对话气泡）

- 用户：右对齐，圆角 20（右下 6）
- 人格：左对齐，顶部人格头像 + 人格色描边，圆角 20（左下 6）
- 背景 `rgba(255,255,255,0.06)`（克制玻璃）；AI 思考中：三点波形动画 + "正在思考…"
- 打字机流式输出；引用来源卡片用人格色左侧细线（**仅引用来源可用细线**）

### 4.3 星系氛围层（deepspace-bg）

- 保留现有 4 层星云径向渐变 + 星空粒子（品牌资产）
- 降饱和 30%，`prefers-reduced-motion` 时停用漂移动画

### 4.4 导航

- 毛玻璃吸顶 `rgba(5,6,15,0.7)` + blur 24px；Logo 左；导航间距 32px；悬停下划线中间展开
- 在线人数徽章：青色脉冲（已有）

---

## 5. 动效系统

| 类型 | 时长 | 缓动 |
|---|---|---|
| 过渡 | 150–250ms | `cubic-bezier(0.22, 1, 0.36, 1)` |
| 弹入 | 400–600ms | `cubic-bezier(0.16, 1, 0.3, 1)` |
| 滚动触发 | 600–800ms | `cubic-bezier(0.22, 1, 0.36, 1)` |

微交互清单：卡片上浮+光晕 / 按钮涟漪 / 链接下划线展开 / 数据 CountUp / AI 思考波形 / 页面淡入（无闪白） / 滚动分层渐入 / 按钮 active `scale(0.97)`（Linear 式触感）。

**`prefers-reduced-motion: reduce` 必须提供降级**（停用漂移/视差/波形，保留淡入）。

---

## 6. 响应式

- 桌面 ≥1024px：完整 Bento + 右侧档案面板
- 平板 481–1024px：隐藏右侧面板可切换
- 手机 <480px：单列堆叠，导航底部标签栏

---

## 7. 无障碍（强制，WCAG 2.1 AA）

- 正文对比 ≥ 4.5:1，大文本 ≥ 3:1
- 全部交互元素可 Tab 访问 + 可见焦点环（`outline: 2px solid #22d3ee`）
- 图标有 aria-label，图片有 alt，人格头像有 `role="img"` + 标签
- 支持 200% 字号缩放不破版

---

## 8. 交付前检查清单

- [ ] 16 色光谱全站一致，无重复无错用
- [ ] 全站只有一个交互强调色（霓虹青）；人格色只用于人格内容
- [ ] 无多色渐变按钮、无卡片左侧竖条、无三栏等分模板
- [ ] 毛玻璃仅用于导航/浮层，卡片为实色表面
- [ ] 深色为默认，浅色无失真
- [ ] 所有交互元素有 hover/active 反馈；active 有 scale(0.97)
- [ ] prefers-reduced-motion 降级生效
- [ ] 键盘全可达，对比度 AA
- [ ] 桌面/平板/手机三端通过视觉测试

---

*DESIGN.md v1 — 2026-08-20 · 基于 Linear/Vercel/Awwwards 2026 实测 + DebateSphere 品牌融合*
