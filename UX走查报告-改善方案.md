# MBTI人格辩论平台 — UX 体验走查报告

> **研究日期**：2026-08-12  
> **研究员**：UX Researcher  
> **环境**：桌面端 (1440×900) + 移动端 (375×667 iPhone SE)  
> **工具**：Playwright 自动化走查 + 人工截图审阅  
> **目标**：基于 20+ 张全屏截图、API 响应数据、控制台日志，评估核心用户旅程（注册→登录→首页→PK→宠物商城→设置）的可用性、可访问性与移动端适配，输出改善方案。

---

## 1. 研究概述

### 1.1 方法
- **自动化走查**：Playwright 模拟真实用户操作，覆盖注册/登录/导航/宠物商城/PK 房间/设置页全流程。
- **多设备测试**：桌面主流分辨率 (1440×900) + 低端手机视口 (375×667)。
- **错误监控**：收集全页面 console 错误与 pageerror。
- **API 冒烟测试**：验证后端 8 只可选宠物、商城 17 件道具全部可用。

### 1.2 关键发现概览

| 优先级 | 问题数量 | 影响 |
|---|---|---|
| 🔴 P0 — 阻断级 | 0 | 无白屏/崩溃，核心功能通 |
| 🟠 P1 — 严重 | 4 | 注册不稳定、新手迷失、宠物入口深、空态过大 |
| 🟡 P2 — 中等 | 6 | 弹窗遮挡、密码不可见、缺少记住我、移动端截断、快捷键误显 |
| 🔵 P3 — 轻微 | 5 | 高对比度按钮、卡片密度、服务器设置喧宾夺主、文案冗余 |

### 1.3 用户旅程地图

```
新访客 → 首页人格大厅 → [登录弹窗] → 浏览人格卡片 → 点击PK房间 → 创建/匹配
                               ↓                                      ↓
                          [注册新用户]                           [像素宠物]
                               ↓                                    [我的宠物]
                          [首屏无引导]                           [选择初始宠物]
                               ↓                                      ↓
                          [不知道下一步]                          [商城/背包/对战]
```

---

## 2. 详细发现

### 🔴 P1 严重级问题

#### P1.1 — 注册 API 边界不稳定（补充测试暴露）
- **现象**：部分用户名（如 `deep_msp...`）触发后端 400 错误 `"用户名至少2个字符，密码至少4个字符"`，但用户名/密码长度均合规。疑似后端验证逻辑有边界 case。
- **影响**：真实用户可能遇到随机注册失败，流失率上升。
- **改善**：审查 `server/routes/auth.js` 中注册验证正则，确认是否误拦截含下划线/数字开头的用户名。
- **截图**：见 `18-宠物商城.png` 登录弹窗内红色错误提示。

#### P1.2 — 首屏无新手引导，用户「迷失」
- **现象**：新用户登录后（截图 `04-登录后状态.png`），页面直接呈现 16 张人格卡片 + 筛选标签 + 搜索框。用户完全不知道：
  1. 人格卡片是干什么的？（点击会怎样？）
  2. 右上角「开始辩论 (0)」数量为 0，无法点击。
  3. 应该先点击哪个人格？
  4. 什么是「匹配测试」？
  5. 宠物商城在哪？怎么获得宠物？
- **影响**：首次使用体验极差，用户可能 30 秒内离开。当前产品缺少「价值确认」——用户未感受到核心乐趣。
- **改善**：增加首次登录引导弹窗/Step-by-step 引导（见第 3 章）。

#### P1.3 — 宠物商城入口深度过深（2 层点击 + 无独立导航）
- **现象**：主导航中无「宠物商城」独立入口。用户必须：
  1. 点击侧边栏「PK房间」
  2. 再点击 PK 房间内的「我的宠物」按钮
  3. 才能进入宠物商城
- **影响**：宠物是产品的核心差异化卖点（像素宠物 + 实时对战），但隐藏在二级入口。用户难以发现宠物系统。
- **改善**：在侧边栏导航增加独立「宠物商城」入口，或在人格大厅增加宠物引导卡片。

#### P1.4 — PK 房间空态占屏比过高，用户缺乏行动点
- **现象**：截图 `07-PK竞技场.png` 显示，空房间时页面 80% 区域为纯黑色空白，仅中央有一行「暂无房间，创建一个吧！」文字，无显眼的创建按钮或引导插画。
- **影响**：用户进入后不知道下一步，可能认为功能损坏。快速匹配按钮（红色）在空房间时无视觉反馈，用户不确定是否正在匹配。
- **改善**：在空态区域增加中央插画 + 大号「创建房间」CTA 按钮 + 快速匹配按钮增加加载/匹配状态提示。

### 🟡 P2 中等级问题

#### P2.1 — 登录弹窗未拦截下层交互，内容重叠
- **现象**：未登录用户点击受保护页面（如宠物商城），页面内容（商城 Tab、宠物选择文本）在弹窗背后加载并可见，导致用户可同时看到内容 + 登录弹窗，产生混淆。
- **截图**：`18-宠物商城.png` 背景清晰显示「像素宠物商城 · 选择你的初始宠物伙伴」内容，但被弹窗遮挡。
- **改善**：登录弹窗应使用 `position: fixed; z-index: 50` 并配合背景遮罩（`backdrop-blur`），阻断下层滚动和点击。

#### P2.2 — 密码输入无可见性切换
- **现象**：登录弹窗（`02-登录弹窗.png`）密码框为纯 `type="password"`，无 👁 图标切换可见/隐藏。用户输错时无法确认内容。
- **改善**：在密码输入框右侧增加密码可见切换按钮。

#### P2.3 — 缺少「记住我」与「自动登录」
- **现象**：每次打开桌面版或手机 App 都需要重新登录。用户需要输入用户名+密码，步数过多。
- **改善**：增加 `remember me` 复选框，将 JWT 持久化到 localStorage（可选），实现自动登录。

#### P2.4 — 移动端顶部导航文字严重截断
- **现象**：截图 `M1-移动首页.png`（375px）显示顶部导航栏「开始辩论 (0)」按钮被右侧截断，只有部分文字可见。侧边栏在移动端占据大量空间（左侧显示图标导航）。
- **改善**：移动端应折叠顶部横向导航为汉堡菜单，或仅保留图标（缩小按钮宽度）。

#### P2.5 — 键盘快捷键文案在移动端无意义
- **现象**：所有页面右上角显示「Ctrl+1~9」快捷键提示，在移动端没有物理键盘，文字浪费空间且误导。
- **改善**：移动端视口下隐藏快捷键提示，或改为触摸手势提示。

#### P2.6 — 设置页「服务器连接」喧宾夺主
- **现象**：截图 `08-设置页.png` 显示「服务器连接」区块位于设置页首屏，包含大量文字说明（>100字）和输入框，但 95% 用户仅使用桌面版，无需配置服务器。
- **改善**：将服务器连接折叠为「高级设置」或仅在使用手机 App 时显示。

### 🔵 P3 轻微级问题

#### P3.1 — 高对比度模式下按钮文字对比度不足
- **现象**：设置页效果预览中，深色按钮上的「主要按钮」文字在深色主题下可能不满足 WCAG AA（4.5:1）。需要实测验证。
- **改善**：检测按钮前景色与背景色对比度，确保始终 ≥ 4.5:1。

#### P3.2 — 人格卡片在移动端每屏仅 2 张，滚动量过大
- **现象**：375px 下 2 列布局（`M1-移动首页.png`），每屏约 2-3 行（4-6 张卡片），用户需要 3-4 次滚动才能看完 16 种人格。
- **改善**：移动端卡片缩小 padding/字号，或在顶部增加人格搜索（已有）作为主要发现方式。

#### P3.3 — 社区广场/匹配推荐/数据统计等页面内容为空
- **现象**：补充走查中，社区广场、匹配推荐、数据统计等页面在登录态下显示大量空白（无内容填充）。新用户无法获得社区反馈或数据价值。
- **改善**：为空态填充示例内容/引导文案，或暂时隐藏未完善的功能入口。

#### P3.4 — 宠物商城 canvas 像素宠物缺少替代文字描述
- **现象**：虽然 canvas 已加 `aria-label`，但描述较简单（如「像素龙 Lv.3」），未提供颜色、形态等细节，视障用户无法感知视觉特征。
- **改善**：扩展 `aria-label` 为「像素龙 — 蓝色身体，喷火动画，当前等级 3」。

#### P3.5 — 搜索框在移动端宽度较窄
- **现象**：移动端搜索框（`M1-移动首页.png`）约 280px，placeholder「搜索人格名称、别名...」可能截断。
- **改善**：移动端搜索框全宽，placeholder 缩短为「搜索人格」。

---

## 3. 改善方案（具体代码/设计建议）

### 3.1 新手引导（最高优先级，解决 P1.2）

**目标**：首次登录后，5 秒内让用户理解「1. 选人格 → 2. 开始辩论 → 3. 获得宠物」的核心循环。

**建议设计**：
- 步骤 1：欢迎浮层 — 「欢迎加入 MBTI 辩论！选择一个人格作为你的辩论搭档，开始你的第一场辩论。」
- 步骤 2：高亮引导 — 在人格大厅中选一张人格卡片（如 INTJ），用 spotlight 高亮 + 箭头提示点击。
- 步骤 3：辩论引导 — 选中人格后，高亮「开始辩论」按钮，提示点击开始。
- 步骤 4：宠物引导 — 辩论结束后，弹出「获得积分！去宠物商城领养你的第一个像素宠物吧！」，提供直达按钮。

**实现方案**：
- 增加 `FirstVisitGuide` 组件，用 `localStorage` 标记 `guide_completed`。
- 使用 `react-joyride` 或自研 spotlight + tooltip 引导组件。
- 在用户完成首次辩论后，自动打开宠物商城。

### 3.2 宠物商城独立导航入口（解决 P1.3）

**建议**：在侧边栏导航增加「宠物商城」独立图标按钮，使用 🐾 或 🎒 图标。  
路径：人格大厅 → 宠物商城（直接可见）。  
在 PK 竞技场中保留「我的宠物」作为快捷入口（已有）。

**实现方案**：在 `App.tsx` 导航列表中增加：
```tsx
{ icon: <PawPrint size={20} />, label: '宠物商城', key: 'pets', component: <PetShop /> }
```

### 3.3 PK 房间空态优化（解决 P1.4）

**建议设计**：空态区域替换为：
- 中央插画/动画（像素宠物对战图标或动态 pixel art 动画）
- 大号文案：「还没有房间，快来开启第一场辩论吧！」
- 两个按钮：「创建房间」（大号紫色 CTA）+ 「快速匹配」（次要高亮）
- 底部显示最近热门话题或推荐辩题作为辅助行动点

**实现方案**：修改 `PKLobby.tsx` 空态区块：
```tsx
<div className="flex flex-col items-center justify-center py-16">
  <PixelPet pet={{ sprite_type: 'dragon' }} size={80} animated />
  <p className="text-lg font-bold mt-4">还没有房间，快来开启第一场辩论吧！</p>
  <div className="flex gap-4 mt-6">
    <button className="px-6 py-3 rounded-lg bg-accent text-white font-bold text-lg hover:scale-105 transition-all">
      + 创建房间
    </button>
    <button className="px-6 py-3 rounded-lg border border-accent text-accent font-bold text-lg hover:bg-accent-10 transition-all">
      ⚡ 快速匹配
    </button>
  </div>
  <p className="text-sm text-secondary mt-4">推荐辩题：「AI 应该拥有创作版权吗？」</p>
</div>
```

### 3.4 登录弹窗遮罩优化（解决 P2.1）

**建议**：增加遮罩层，阻止下层交互和滚动。同时添加 Esc 键关闭。

**实现方案**：在登录弹窗外层增加：
```tsx
<div className="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm flex items-center justify-center"
     onClick={() => setShowLogin(false)}>
  <div className="bg-bg-primary rounded-2xl p-6 max-w-md w-full shadow-2xl"
       onClick={e => e.stopPropagation()}>
    {/* login form */}
  </div>
</div>
```

### 3.5 密码可见切换 + 记住我（解决 P2.2 + P2.3）

```tsx
// 密码框
<div className="relative">
  <input type={showPassword ? 'text' : 'password'} className="..." />
  <button type="button" aria-label={showPassword ? '隐藏密码' : '显示密码'}
          className="absolute right-3 top-1/2 -translate-y-1/2"
          onClick={() => setShowPassword(!showPassword)}>
    {showPassword ? <EyeOff size={16} /> : <Eye size={16} />}
  </button>
</div>

// 记住我
<label className="flex items-center gap-2 cursor-pointer">
  <input type="checkbox" checked={rememberMe} onChange={e => setRememberMe(e.target.checked)} />
  <span className="text-sm">记住我</span>
</label>
```

**自动登录**：登录成功后将 JWT token 存入 `localStorage`（当前可能使用 `sessionStorage`）。App 初始化时读取 token 并自动验证登录态。

### 3.6 移动端顶部导航折叠（解决 P2.4）

**建议**：在 `md:`（768px）以下断点，将顶部导航栏折叠为：
- 左侧：品牌名 + 汉堡菜单图标
- 右侧：登录/用户头像
- 点击汉堡菜单后展开底部/侧边抽屉式导航菜单

**实现方案**：使用 `hidden md:flex` 类控制顶部导航栏在移动端隐藏，同时添加移动端专用的底部导航栏或侧边栏抽屉。

### 3.7 设置页服务器连接折叠（解决 P2.6）

**建议**：将服务器连接区块改为可折叠的「高级设置」区块，默认折叠。仅在移动端（用户可能用手机 App）时默认展开。

```tsx
<details className="group">
  <summary className="flex items-center justify-between cursor-pointer py-2">
    <span>高级设置（手机连接）</span>
    <ChevronDown className="group-open:rotate-180 transition-transform" size={16} />
  </summary>
  <div className="pt-2">{/* 服务器连接内容 */}</div>
</details>
```

### 3.8 可访问性补充（P3.4）

扩展 PixelPet canvas 的 aria-label：
```tsx
const arialabel = `${pet.name || '像素宠物'} — ${spriteTypeDesc[pet.sprite_type]}，当前等级 ${pet.level || 1}`;
<canvas role="img" aria-label={arialabel} />
```

---

## 4. 移动端体验专项

| 问题 | 影响 | 建议 |
|---|---|---|
| 导航栏文字截断 | 用户无法识别按钮 | 折叠为汉堡菜单 |
| 快捷键提示存在 | 无意义文案浪费空间 | 移动端隐藏 |
| 人格卡片密度低 | 滚动过多 | 缩小卡片 padding/字号 |
| 搜索框宽度不足 | 输入不便 | 全宽搜索 |
| 侧边栏始终显示 | 挤压内容区域 | 移动端收起为底部导航栏 |
| 无触摸手势优化 | 操作效率低 | 卡片支持左滑收藏/右滑辩论 |

---

## 5. 快速改进清单（按实施难度排序）

| 难度 | 改进项 | 预计影响 | 文件 |
|---|---|---|---|
| ⭐ | 增加密码可见切换 | 登录体验 | 登录弹窗组件 |
| ⭐ | 折叠服务器连接设置 | 设置页清晰度 | SettingsPage.tsx |
| ⭐ | 隐藏移动端快捷键提示 | 移动端整洁 | App.tsx |
| ⭐⭐ | 优化登录弹窗遮罩 | 防误触 | 登录弹窗组件 |
| ⭐⭐ | 增加宠物商城独立导航 | 宠物发现率 | App.tsx 侧边栏 |
| ⭐⭐ | 自动登录 + 记住我 | 留存率 | useAuth.tsx |
| ⭐⭐⭐ | PK 房间空态优化 | 用户参与度 | PKLobby.tsx |
| ⭐⭐⭐ | 移动端顶部导航折叠 | 移动端可用性 | App.tsx + 新 BottomNav 组件 |
| ⭐⭐⭐ | 新手引导系统 | 首次体验/留存 | 新增 FirstVisitGuide 组件 |
| ⭐⭐⭐ | 修复注册 API 边界 case | 注册成功率 | server/routes/auth.js |

---

## 6. 附录

### 6.1 截图索引

| 截图 | 描述 | 发现关联 |
|---|---|---|
| `01-首页.png` | 桌面首页（未登录） | 视觉层级正常 |
| `02-登录弹窗.png` | 登录模态框 | P2.2 密码不可见 |
| `03-登录已填写.png` | 填写用户名密码后 | 表单交互正常 |
| `04-登录后状态.png` | 登录后首页 | P1.2 无新手引导 |
| `05-遍历后.png` | 遍历导航后 | 导航完整 |
| `06-宠物商城.png` | 退出登录后的页面 | 实际为设置页（非登录态） |
| `07-PK竞技场.png` | PK 房间空态 | P1.4 空态过大 |
| `08-设置页.png` | 设置页完整 | P2.6 服务器连接喧宾夺主 |
| `M1-移动首页.png` | 移动端首页 | P2.4 导航截断 |
| `M2-移动菜单.png` | 移动端无汉堡菜单 | 无响应式菜单 |
| `18-宠物商城.png` | 登录弹窗遮挡宠物商城 | P2.1 弹窗未拦截 |
| `17-PK房间.png` | 同宠物商城被遮挡 | 登录弹窗问题复现 |

### 6.2 技术验证结果

- ✅ 前端 TypeScript 编译零错误
- ✅ 后端 8 只可选宠物 API 正常返回（`selectablePets` 含 cat/dog/bird/slime/dragon/bunny/fox/penguin）
- ✅ 商城 17 件道具 API 正常
- ✅ 桌面端 console 零错误（第一次走查）
- ⚠️ 补充走查中注册 API 出现 400 边界 case（需排查）
- ✅ 移动端可访问性：已加 `aria-label`、`role="tab"` 等语义化标签
