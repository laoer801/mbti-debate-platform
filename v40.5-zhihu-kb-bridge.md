# v40.5 升级 —— 知乎用户内容/收藏 API 接入知识库

> **一句话**：用户点开 KnowledgeLibrary，能 1 键把自己的知乎**回答 / 文章 / 视频 / 收藏 / 整个收藏夹**抓进知识库，AI 辩手下一场就能引用。

---

## 0. 现状止血点（v40.4 → v40.5 之前的痛点）

老用户反馈：
1. 平台知识库里全是新闻 / 系统灌的资料，**没我自己的思想**
2. 收藏夹里我攒的好文，**没法直接喂给 AI 学**
3. 我得意的回答 / 文章，**没法变成"我能引用的素材"**

v40.5 解法：知乎开放平台 4 个用户侧 API **直接打通进 KnowledgeLibrary**，批量入库。

---

## 1. 改造路径

### 1.1 后端 —— 4 个新路由（与 v40 搜索 / 直答 / 热榜完全一致的封装风格）

| 路由 | 对应知乎 API | 用途 |
|---|---|---|
| `GET /api/zhihu/user/contents` | `GET /api/v1/user/contents` | 拉本人/被授权用户全部内容 |
| `GET /api/zhihu/user/collections` | `GET /api/v1/user/collections` | 拉本人/被授权用户收藏 |
| `GET /api/zhihu/user/favlists` | `GET /api/v1/user/favlists` | 拉收藏夹列表 |
| `GET /api/zhihu/favlist-contents` | `GET /api/v1/user/favlist_contents` | 拉某个收藏夹里的全部内容 |
| `POST /api/zhihu/import-to-kb` | （自建） | 把任意 item 转成 `cloudkb.upload` 的 payload，前端批量 POST 上传 |

> **OAuth 支持**：所有用户侧路由都兼容 `X-OAuth-Token` header，没有就走本人数据。

### 1.2 前端 —— KnowledgeLibrary 顶部插一块新面板

**新增 `client/src/components/ZhihuSourcesPanel.tsx`** —— 一站式面板：

- 4 入口 tab：**我的内容 / 我的收藏 / 收藏夹内容 / 搜索**
- 子筛选：内容类型（all/answer/article/zvideo/pin/question）
- 收藏夹点选
- 多选 + 全选 + 批量按钮
- 进度提示 + 失败明细

### 1.3 现有零改动

- `server/routes/cloudkb.js` —— 复用了 `/api/cloudkb/upload`，**完全不用动**
- `client/src/utils/cloudKbClient.ts` —— 完全不用动
- 现有 KnowledgeLibrary / PersonaChat / DebateRoom —— 完全不用动

---

## 2. 落地文件清单（已落到 `D:/mbti-debate-platform/`）

| 文件 | 改动 |
|---|---|
| `server/zhihu.js` | +4 个端点常量，+4 个 API 封装函数，+`itemToKbPayload()` 转码器 |
| `server/routes/zhihu.js` | +5 个路由（4 个 user 侧 + import 转码） |
| `client/src/utils/zhihuClient.ts` | +4 个 fetch，+`toKbPayload()` +`importItemsToKb()` 批量导入，+4 个 React Hook（含 5 分钟缓存） |
| `client/src/components/ZhihuSourcesPanel.tsx` | 新增（一站式面板组件） |

---

## 3. 5 分钟上手

### 3.1 现有 OAuth 接入（如已开）

无需额外配置，`ZHIHU_ACCESS_SECRET` 已配的情况下，**直接拉到本人数据**（回答、文章、想法、问题、视频）。

### 3.2 想看别人的知乎内容（高级）

告诉用户把他自己的知乎 OAuth token 发给你，前端请求时 header 加：

```
X-OAuth-Token: <oauth-access-token>
```

或：

```
GET /api/zhihu/user/contents?limit=20  -H "X-OAuth-Token: xxx"
```

服务端会自动带上透传给知乎平台。

### 3.3 UI 接入（KnowledgeLibrary.tsx 顶端加 1 行）

```tsx
import { ZhihuSourcesPanel } from './ZhihuSourcesPanel'

// 在 KnowledgeLibrary 顶部插入：
<ZhihuSourcesPanel userId={currentUserId} onImported={refreshCloudKb} />
```

---

## 4. 自检 curl（重启服务后）

```bash
# 1. 看启用 + 端点
curl http://localhost:3001/api/zhihu/status

# 2. 用户内容（拉本人）
curl "http://localhost:3001/api/zhihu/user/contents?contentType=answer&limit=5"

# 3. 收藏夹列表
curl "http://localhost:3001/api/zhihu/user/favlists?limit=10"

# 4. 某收藏夹内容
curl "http://localhost:3001/api/zhihu/favlist-contents?favlistUrlToken=123&limit=10"

# 5. 一次性转码成 KB payload
curl -X POST http://localhost:3001/api/zhihu/import-to-kb \
  -H "Content-Type: application/json" \
  -d '{"items":[{"Title":"测试","Summary":"x","Url":"https://...","ContentType":"answer","LikeCount":10}],"source":"zhihu:user-contents"}'
```

返回：

```json
{
  "enabled": true,
  "source": "zhihu:user-contents",
  "count": 1,
  "payloads": [{ "title": "测试", "text": "...", "source_type": "zhihu:user-contents", "external_url": "...", "metadata": {...} }],
  "hint": "请循环调用 POST /api/cloudkb/upload，body = payloads[i] + userId"
}
```

---

## 5. 与 v40.4 / v40.3 升级的协同

| 链路 | 走法 |
|---|---|
| 知乎条目 → cloudkb.upload | import-to-kb 转码 → 客户端循环 upload（前端用 React Hook 处理） |
| 入库后 AI 引用 | cloudkb.js 内置 FTS5 + BM25，辩论 prompt 自动检索（已 v40.3 配好） |
| 跨 4 个 API 共享 user_id/token | 全部走服务端代理，secret 不出后端 |
| OAuth 限流（30001/30002） | 自动捕获到 503 错误返回，前端显示明确的"频率限制"提示 |

---

## 6. v40.6 路线图（可选）

1. **增量同步**：每晚自动把用户最近 10 条新收藏 + 用户内容 sync 到 KB
2. **反作弊频率控制**：单用户每天最多入库 100 条
3. **标签/分类自动生成**：基于用户收藏夹分组，AI 自动归类

---

## 7. 安全说明（已做到）

1. ✅ Access Secret 永远留在服务端 `.env`，从不传给前端
2. ✅ OAuth Token 由前端 → 我方后端 → 知乎，不绕路
3. ✅ 没有管理员权限隔离；普通用户只能拉"本人公开"或"被授权用户公开"
4. ✅ 所有用户侧端点都包在 `if (!zhihuEnabled())` 兜底下，**没配 secret 时 503**

---

## 8. 知识库闭环（自画）

```
知乎收藏 / 回答 / 文章（API）
      ↓ ZhihuSourcesPanel 多选
      ↓ importItemsToKb
   /api/cloudkb/upload
      ↓ chunk + FTS5 索引
   数据库
      ↓ 辩论时 retrieveVideoKnowledge / retrieveNewsKnowledge 自动取
   AI 辩手发言（带 [来源] 角标）
```

闭环形成：用户**收藏什么，AI 就学什么**。下回辩论时，AI 引用你的知乎论点直接顶你，**真的"把你自己的思想变成辩论武器"**。
